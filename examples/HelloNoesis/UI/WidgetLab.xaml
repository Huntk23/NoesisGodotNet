<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid.Background>
    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
      <GradientStop Offset="0" Color="#FF2A1A3E"/>
      <GradientStop Offset="1" Color="#FF0E0A1E"/>
    </LinearGradientBrush>
  </Grid.Background>

  <!-- Minimal templates so this works without the Noesis theme package.
       (Theme integration is a separate roadmap item.) -->
  <Grid.Resources>
    <!-- Implicit ScrollViewer template: TextBox injects a ScrollViewer as its
         PART_ContentHost, and without ANY template it renders as Noesis's pink
         fallback block — hiding text, caret, and selection. -->
    <Style TargetType="ScrollViewer">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollViewer">
            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                    Margin="{TemplateBinding Padding}"/>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="LabTextBox" TargetType="TextBox">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="CaretBrush" Value="White"/>
      <Setter Property="SelectionBrush" Value="#5548A9D8"/>
      <Setter Property="FontSize" Value="16"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="Bg" Background="#22FFFFFF" BorderBrush="#66FFFFFF"
                    BorderThickness="1" CornerRadius="4" Padding="8,6">
              <ScrollViewer x:Name="PART_ContentHost"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Bg" Property="BorderBrush" Value="#FF7FD4FF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="LabCheckBox" TargetType="CheckBox">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontSize" Value="16"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal">
              <Border Width="20" Height="20" Background="#22FFFFFF"
                      BorderBrush="#66FFFFFF" BorderThickness="1" CornerRadius="3">
                <Path x:Name="Check" Data="M4,10 L8,14 L16,5" Stroke="#FF7FD4FF"
                      StrokeThickness="2" Visibility="Hidden"/>
              </Border>
              <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Grid.Resources>

  <!-- Continuous animation: verifies smooth per-frame rendering -->
  <Grid.Triggers>
    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
      <BeginStoryboard>
        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
          <DoubleAnimation Storyboard.TargetName="Pulse"
                           Storyboard.TargetProperty="Opacity"
                           From="0.15" To="1.0" Duration="0:0:1"/>
        </Storyboard>
      </BeginStoryboard>
    </EventTrigger>
  </Grid.Triggers>

  <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="340">
    <TextBlock Text="WIDGET LAB" FontSize="32" FontWeight="Bold" Foreground="White"
               HorizontalAlignment="Center"/>
    <Rectangle x:Name="Pulse" Height="3" Fill="#FF7FD4FF" Margin="0,6,0,24"/>

    <TextBlock Text="Type your name (tests keyboard + focus):"
               Foreground="#AAFFFFFF" Margin="0,0,0,4"/>
    <TextBox Style="{StaticResource LabTextBox}"
             Text="{Binding PlayerName, UpdateSourceTrigger=PropertyChanged}"/>

    <TextBlock Text="{Binding Greeting}" FontSize="20" Foreground="#FF7FD4FF"
               Margin="0,10,0,20" HorizontalAlignment="Center"/>

    <CheckBox Style="{StaticResource LabCheckBox}" Content="Enable awesome mode"
              IsChecked="{Binding AwesomeMode}"/>

    <TextBlock Text="{Binding AwesomeStatus}" Foreground="#AAFFFFFF" Margin="0,8,0,0"/>
  </StackPanel>
</Grid>
